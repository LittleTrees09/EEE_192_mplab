/*
 * Intentionally left out for now.
 *
 * Your 5-IR TCRT5000 line array is being treated as pure digital GPIO inputs,
 * so I2C adds zero value at this stage. Add a real i2c.c later only when you
 * actually wire an I2C peripheral.
 */
